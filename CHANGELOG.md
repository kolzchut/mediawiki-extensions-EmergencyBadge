# Changelog

## 0.1.1

- Added i18n support (`namemsg`, `descriptionmsg`) per MediaWiki standards
- Renamed CSS class from `.emergency-badge` to `.ext-emergencyBadge` per naming conventions
- Removed unused `wikimedia/jquery` ESLint config from resources
- Removed unused `baseline-browser-mapping` dev dependency
- Updated `minus-x` to 2.0.1
